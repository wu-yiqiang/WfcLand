<Page x:Class="WfcLand.Views.PasteLists"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WfcLand.Views"
      xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
      xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="PasteLists">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Width="40"
                Height="40"
                AutomationProperties.Name="Add"
                Click="HandleAdd">
            <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Add}" />
        </Button>
        <ui:ScrollViewerEx Grid.Row="1" VerticalAlignment="Top"
                           HorizontalAlignment="Left"
                           IsScrollAnimationEnabled="True"
                           VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ItemsControlLists"
                          Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ikw:SimpleStackPanel Spacing="4" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:SettingsCard Header="{Binding}"
                                         IsEnabled="True">
                            <Button Width="40"
                                    Height="40"
                                    AutomationProperties.Name="Copy"
                                    Tag="{Binding}"
                                    Click="HandleCopy">
                                <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Copy}" />
                            </Button>
                        </ui:SettingsCard>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ui:ScrollViewerEx>
    </Grid>
</Page>